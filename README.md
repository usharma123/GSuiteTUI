# GSuiteTUI

This repository contains the `tui-suite` terminal UI for Google Calendar, Gmail, and Drive.

## Getting Started
From the repo root:
```bash
cd tui-suite
cargo run
```

See `tui-suite/README.md` for full setup, configuration, and feature details.
